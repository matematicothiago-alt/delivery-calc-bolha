jobs:
  build:
    docker:
      - image: cimg/android:2024.01
    environment:
      ANDROID_HOME: /opt/android/sdk
    steps:
      - checkout
      - run:
          name: Build Debug APK
          command: ./gradlew assembleDebug --no-daemon --stacktrace -x lint
