version: 2.1
jobs:
  build:
    docker:
      - image: cimg/openjdk:17.0
    steps:
      - checkout
      - run: chmod +x gradlew
      - run: ./gradlew check
workflows:
  build:
    jobs:
      - build
