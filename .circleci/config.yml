version: 2.1
jobs:
  build:
    docker:
      - image: cimg/android:2024.01
    environment:
      ANDROID_HOME: /opt/android/sdk
    steps:
      - checkout
      - run: gradle assembleDebug --no-daemon -x lint
workflows:
  build:
    jobs:
      - build
